# URL of the shared LUMEN backend (Lumen-backend) — the same API the mobile
# app uses. Used as the Vite dev-server proxy target for /api and /uploads,
# and as the WebSocket endpoint.
VITE_API_BASE_URL=http://localhost:3000
VITE_WS_URL=http://localhost:3000
