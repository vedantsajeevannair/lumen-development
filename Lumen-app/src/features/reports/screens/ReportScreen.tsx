import { Component as PremiumScreen } from "@/components/ui";

export default function ReportScreen() {
  return (
    <PremiumScreen
      badge="Reports"
      description="Browse civic report activity with a premium list rhythm and strong status hierarchy."
      primaryActionLabel="New report"
      secondaryActionLabel="Refresh"
      subtitle="Clear summaries and natural card motion for every report state."
      title="Reports"
    />
  );
}
