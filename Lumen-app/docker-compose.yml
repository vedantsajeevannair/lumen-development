version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
    restart: always

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    restart: always
