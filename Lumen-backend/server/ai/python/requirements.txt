fastapi>=0.110.1
uvicorn>=0.29.0
onnxruntime>=1.18.0
opencv-python-headless>=4.9.0.80
numpy>=2.1.0
Pillow>=10.3.0
requests>=2.31.0
httpx>=0.27.0
python-dotenv>=1.0.1
pydantic>=2.6.4
pydantic-settings>=2.2.1

# boto3 is intentionally absent: it is only needed for MODEL_S3_URI, which the
# Cloud Run image does not use (the weights are baked in), and it costs ~90MB.
# `pip install boto3` if you deploy somewhere that fetches weights from S3.
